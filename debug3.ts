import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const teachers = await prisma.user.findMany({ where: { role: 'TEACHER' } }); const workflows = await prisma.evaluationWorkflow.findMany({ where: { assignedTeacherId: { in: teachers.map(t => t.id) } }, include: { examPaper: { include: { grades: true } } } }); console.log('Workflows found:', workflows.length); } main().catch(console.error).finally(() => prisma.$disconnect());
